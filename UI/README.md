# Framework to generate synthetic text data using Generative AI technologies / available model

## Installing, Configuring and Launching.

Execute the following commands:

```
git clone <project-url>
```